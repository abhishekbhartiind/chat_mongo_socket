* -> Mapping username, socket_id (UserSocketMapping) 
* --- update if previous is there
####
-----------------------------------------------
* -> GroupId => list of (userId, groupId) Table
* -> message, groupId, name == chat
* --> message, list of userId
* => list of socket_id

------------------------------------------------------
group_id, message
call form post 
===>